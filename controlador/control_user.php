<?php
include_once "modelo/personas.php";
/* we include the model that allows inserting the information to the database */

if(isset($_POST['cc'])&&isset($_POST['fullname'])&&isset($_POST['password'])&&isset($_POST['email'])){
     // we validate that the text boxes are not empty, and we assign the values of them to the variables 
}
?>